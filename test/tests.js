export function displayStats(waterfrontCount, skylineCount, castleCount) {
    return `You've changed the waterfront ${waterfrontCount} times, the skyline ${skylineCount} times, and the castle ${castleCount} times.`;
}